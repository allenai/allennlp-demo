#!/bin/bash
set -eu

dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

compose() {
    docker-compose -f "$dir"/../docker-compose.ui-only.yaml "$@"
}

cmd="${1:-up}"
if [[ "$cmd" == "down" ]]; then
    compose down
else
    compose up --build
fi
