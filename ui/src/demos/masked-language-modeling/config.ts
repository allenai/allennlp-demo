import { DemoConfig } from '../../tugboat';

export const demoConfig: DemoConfig = {
    group: 'Other',
    title: 'Masked Language Modeling',
    path: '/masked-lm',
    order: 3,
    status: 'hidden',
};
