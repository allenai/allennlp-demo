import { DemoConfig } from '../../tugboat/lib';

export const config: DemoConfig = {
    group: 'Other',
    title: 'Masked Language Modeling',
    path: '/masked-lm',
    order: 3,
    status: 'hidden',
};
