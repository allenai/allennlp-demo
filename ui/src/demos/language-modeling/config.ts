import { DemoConfig } from '../../tugboat';

export const demoConfig: DemoConfig = {
    group: 'Other',
    title: 'Language Modeling',
    path: '/next-token-lm',
    order: 2,
    status: 'hidden',
};
