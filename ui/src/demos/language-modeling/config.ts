import { DemoConfig } from '../../tugboat/lib';

export const config: DemoConfig = {
    group: 'Other',
    title: 'Language Modeling',
    path: '/next-token-lm',
    order: 2,
    status: 'hidden',
};
