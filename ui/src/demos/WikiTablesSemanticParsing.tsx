import { DemoConfig } from '../tugboat';

export const demoConfig: DemoConfig = {
    demoGroup: 'Semantic parsing',
    title: 'WikiTables Semantic Parsing',
    routePathOverride: 'wikitables-parser',
    order: 1,
    status: 'hidden',
};
