import { DemoConfig } from '../../tugboat';

export const demoConfig: DemoConfig = {
    group: 'Contributing',
    title: 'User Contributed Models',
    path: '/user-models',
    order: 1,
    status: 'hidden',
};
