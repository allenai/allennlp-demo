import { DemoConfig } from '../../tugboat/lib';

export const config: DemoConfig = {
    group: 'Contributing',
    title: 'User Contributed Models',
    path: '/user-models',
    order: 1,
    status: 'hidden',
};
