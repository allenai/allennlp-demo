import * as slug from './slug';
import * as path from './Path';
import * as link from './Link';
import * as controller from './Controller';

export const Share = { ...slug, ...path, ...link, ...controller };
